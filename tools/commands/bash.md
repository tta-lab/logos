Standard shell environment.

Available: git, jq, curl, python3, wc, sort, uniq, sed, awk.
Piping works: cmd1 | cmd2 | cmd3.

- Read-only filesystem (writes only to /tmp)
- Session-scoped /tmp directory
- 30-second timeout
- Network access enabled
